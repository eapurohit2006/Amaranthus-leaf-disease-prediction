# Node.js Backend Proxy

This is an optional Node.js proxy that forwards requests to the Python FastAPI backend.

## Setup

```bash
npm install
npm start
```

Or use the Python backend directly at http://localhost:8000











